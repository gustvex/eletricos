{
  "name": "@eletricos/server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API de leads e servidor de producao do site",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch src/index.js",
    "start": "node --env-file-if-exists=.env src/index.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@eletricos/shared": "*",
    "compression": "^1.7.5",
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
